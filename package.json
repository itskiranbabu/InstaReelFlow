{
  "name": "instareelflow",
  "version": "1.0.0",
  "description": "A modern Instagram Reels-style video sharing platform",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-deps": "npm install && cd client && npm install && cd ../server && npm install"
  },
  "keywords": ["react", "nodejs", "mongodb", "video-sharing", "reels"],
  "author": "InstaReelFlow Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}